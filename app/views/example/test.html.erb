<h1>First experiment with erb</h1>
<p>Addition:
  <%= 1 + 1 %>
</p>

<p>Loop from 0 to 5:
  <% (0..5).each do |i| %>
    <%= "#{i}, " %>
  <% end %>
</p>

<p>
  The current time is <%= @current_time %>
</p>


...
<%= render partial: 'shared/footer', locals: {start_year: '2000'} %>
<%= render partial: 'shared/footer', locals: {start_year: nil} %>
<%= render 'shared/footer' %>

<% (1..5).each do |i| %>
  <p>
    <b><%= "Rating for #{i} stars: " %></b> <%= render_stars(i) %>
  </p>
<% end %>

<%= debug(@current_time) if Rails.env.development? %>
<%= debug(params) if Rails.env.development? %>
<%= debug(assigns) if Rails.env.development? %>
<%#= debug(controller) if Rails.env.development? %>
<%#= debug(flash) if Rails.env.development? %>
<%#= debug(request) if Rails.env.development? %>
<%#= debug(response) if Rails.env.development? %>
<%#= debug(session) if Rails.env.development? %>
<%= console if Rails.env.development? %>